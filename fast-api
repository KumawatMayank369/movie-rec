from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI()

# ── ADD THIS ──
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://kumawatmayank369.github.io"],          # or ["https://yourusername.github.io"]
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
